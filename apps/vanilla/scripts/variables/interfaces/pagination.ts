/** Data for updating the pagination state. */
export interface PaginationUpdateData {

  /** Current page number. */
  readonly currentPage: number;

  /** Total number of pages. */
  readonly totalPages: number;
}
